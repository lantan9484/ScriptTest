using System.Collections; using System.Collections.Generic; using UnityEngine;   // Use this for initialization public class Test : MonoBehaviour { 	// Update is called once per frame 	void Start(){ 		int[] array = { 1, 5, 9, 15, 21 };  		//配列の各要素の値を順番に表示 		Debug.Log("配列の各要素の値を順番に表示"); 		for (int i = 0; i < array.Length; i++) { 			Debug.Log (array [i]); 		}  		//配列の各要素の値を逆順に表示 		Debug.Log("配列の各要素の値を逆順に表示"); 		for (int j = array.Length - 1; j >= 0; j--){ 			Debug.Log (array [j]); 		} 	}  	void Update () {  	} }